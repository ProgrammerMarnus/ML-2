FUTURE STEPS — INSTITUTIONAL QUANT RESEARCH ENGINE
Current as of: 2026-09-14

CURRENT POSITION
The project remains RESEARCH_ONLY. H-001 has completed a corrected,
hypothesis-specific confirmation and was rejected: net Sharpe 0.787318,
placebo percentile 0.35, adjusted p-value 0.6667. H-002 and H-003 have not
produced valid trials because their preregistered data contracts are not
available to the scalar single-asset runner. The runtime correctly refuses
generic price/volume proxy executions.

Engineering defects E01-E10 are fixed and regression-tested. The latest full
suite passed 360 tests with no failures or teardown errors; three PaperBroker
tests added afterward also pass, and the repository now collects 363 tests.
PaperBroker hardening phase 4.1 is complete, including order lifecycle,
safeguards, persistence/restart integrity, and a 250-order load smoke test.
This simulator evidence is not paper-market or live-broker evidence.

PRIORITY ORDER

1. Resolve the H-002/H-003 scientific data decision.
   - H-002 needs a cross-sectional small/mid-cap universe plus liquidity inputs.
   - H-003 needs a multi-asset panel plus VIX/term-structure inputs.
   - Either implement those exact contracts or close the hypotheses before
     creating a newly preregistered, scalar-compatible family.
   - Do not run substitutes under the existing H-002/H-003 identifiers.

2. Preserve H-001 as a rejecting result.
   - Keep the corrected config, immutable experiment record, exact dataset
     identity, and placebo output together.
   - Do not tune the hypothesis against the observed confirmation period.

3. Complete paper execution accounting (phase 4.2/4.3).
   - Verify realized and unrealized P&L attribution, buying power/margin rules,
     reconciliation, exchange-hours behavior, stale-mark handling, alerts, and
     operator controls in end-to-end scenarios.

4. Build observed paper-validation evidence only after a strategy reaches the
   required research state.
   - Use unique sessions and event-derived evidence.
   - Test restarts, duplicate requests, partial fills, stale data, kill-switch
     persistence, and emergency flattening.
   - Simulator calls alone cannot promote a strategy.

5. Establish repeatable CI evidence.
   - Run the current 363-test collection in isolated state.
   - Retain machine-readable results and separate expected synthetic-window
     warnings from actionable warnings.

6. Defer any live broker adapter and capital deployment.
   - Select and integrate a broker only after research and observed-paper gates
     pass and operational ownership, credentials, monitoring, and rollback are
     defined.

DO NOT DO
- Do not reinterpret the invalid early H-001/H-002 proxy runs as hypothesis
  evidence.
- Do not loosen placebo gates or spend additional trials to chase a pass.
- Do not claim PAPER_READY, PAPER_VALIDATED, or LIVE_ELIGIBLE from simulator
  tests.
- Do not send live orders or provide credentials while status is RESEARCH_ONLY.

CANONICAL STATUS
See CURRENT_PROJECT_STATUS.md and LIVE_TRADING_READINESS_CHECKLIST.txt. Older
audit, milestone, and execution documents are historical snapshots and are
labelled accordingly.
